// Placeholder — será substituído pelos tipos reais gerados a partir do schema do Supabase:
//
//   bunx supabase gen types typescript --project-id <PROJECT_ID> > src/lib/supabase/types.ts
//
// Rode esse comando depois que as migrations em supabase/migrations forem aplicadas
// ao projeto (npx supabase db push), para manter o client 100% tipado.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
